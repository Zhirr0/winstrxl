import React from 'react'

const ProjectsCard = ({index}) => {
  return (
    <div className={`projects-card projects-card-${index}`}>
      {/* Empty div with background color */}
    </div>
  )
}

export default ProjectsCard